#include<stdio.h>
int main()
{
	struct simp 
	{
		int i = 6;
		char city[] = "chennai";
	};
	struct simp s1;
	printf("%d",s1.city);
	printf("%d", s1.i);
	return 0;
}
OUTPUT
The given code will not compile successfully because the syntax for initializing struct members inside the struct definition is not valid in C.
